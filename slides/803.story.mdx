import { Meta, Story, Preview } from '@storybook/addon-docs/blocks'
import Sprites from '../src/Sprites'
import DemoResizer from '../src/DemoResizer'
import TruncatedList from '../src/TruncatedList'
import items from '../src/TruncatedList.data'

<Meta title="Demo/TruncatedList" component={TruncatedList} />

### Truncated horizontal menu

<Story name="TruncatedList">
  <DemoResizer init='240px'>
    <TruncatedList
      items={
        items.map(item => ({ item, order: Math.random() }))
          .sort((a, b) => a.order - b.order)
          .map(({ item }) => item)
      }
    />
  </DemoResizer>
</Story>
