import { Meta, Story } from '@storybook/addon-docs/blocks'
import Sprites from '../src/Sprites'
import DemoResizer from '../src/DemoResizer'
import Grid from '../src/Grid'
import ItemCard from '../src/ItemCard'
import cats from '../src/cats'

<Meta
  title="Demo/Composition of responsive components"
  component={Grid}
  decorators={[
    storyFn => (
      <>
        <Sprites />
        {storyFn()}
      </>
    )
  ]}
/>

### Composition of responsive components

<Story name="Grid">
  <DemoResizer init='240px'>
    <Grid>
      {cats.map(cat => <ItemCard key={cat.title} {...cat} />)}
    </Grid>
  </DemoResizer>
</Story>
