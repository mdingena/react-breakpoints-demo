import { Meta, Story } from '@storybook/addon-docs/blocks'
import Sprites from '../src/Sprites'
import Resizer from '../src/Resizer'
import Grid from '../src/Grid'
import ItemCard from '../src/ItemCard'
import cats from '../src/cats'

<Meta
  title="Demo/Composition of responsive components"
  component={Grid}
  decorators={[
    storyFn => (
      <>
        <Sprites />
        {storyFn()}
      </>
    )
  ]}
/>

### Composition of responsive components [[Source](https://github.com/mdingena/react-breakpoints-demo/tree/master/src/Grid)]

<Story name="Grid">
  <Resizer init='240px'>
    <Grid>
      {cats.map(cat => <ItemCard key={cat.title} {...cat} />)}
    </Grid>
  </Resizer>
</Story>
