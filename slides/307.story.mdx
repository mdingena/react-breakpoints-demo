import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="Challenges of responsive design/Separation of concerns" />

For us to arrive at 630 pixels for the media query, we must be aware of **margins and paddings added by components surrounding our container**.

But because of the way we separate the code for each of our components, that's not always obvious from looking at just our current component's CSS.
