import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="React Breakpoints/Steps" />

To achieve a component like this, we require at least:

* a **ResizeObserver** to be able to observe an element;
* a component to **observe** an element and **store** the observation in a React context; and
* a React hook to **consume** the observation within that context.
