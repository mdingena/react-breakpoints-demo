import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="Using ResizeObservers/Recreating element queries" />

But how useful is ResizeObserver, really?

I've wondered for a while how I could use this size information.

If we want to create something to the effect of element queries, **how would we use a ResizeObserver to achieve it?**
