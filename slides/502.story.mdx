import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="The ResizeObserver API/Previous JS solutions" />

We've had JavaScript solutions before, but their performance was inferior to CSS media queries.

Examples of JS solutions are:

* polling elements on their size;
* sentinel elements to measure an element's corners;
* using resize events.

ResizeObservers solve a common issue with those solutions:

# <center>Performance. <big><big>🌋</big></big></center>
