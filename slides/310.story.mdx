import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Challenges of responsive design/The onus is on us" />

Because everything hinges on the size of the window, the onus is on the frontend developer to work their way backwards from the viewport to the container.

Wouldn't it be easier if we could write @media queries for elements?
