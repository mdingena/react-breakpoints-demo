import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="Element queries/Top requested CSS improvement" />

Element queries (or "container queries") continue to be **one of the top requested improvements to CSS**.

It's this really shiny thing we want but doesn't exist.
