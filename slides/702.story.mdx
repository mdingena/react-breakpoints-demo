import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="React Breakpoints/The goal" />

### The goal

My goal was to create a component that can respond to another element's size.

Responding to your own size is probably a Bad Idea ™️ because of potential circular dependencies.
